# template-svelte-electron



## Installation
- git clone
- npm install
- disable hmr in webpack.renderer.config.js for some weird reason (HMR will still work for some even weirder reason)
- npm run dev to start
